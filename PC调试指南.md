# PC调试指南

本项目支持在不连接ESP32眼镜的情况下，通过本地电脑的摄像头进行功能调试和开发。本指南将详细介绍如何使用这一功能。

## 功能概述

- 通过本地摄像头模拟眼镜摄像头输入
- 无需连接ESP32设备即可测试视觉算法
- 支持调整摄像头索引和分辨率
- 保持与眼镜模式相同的功能体验

## 启动方法

### 基本启动命令

使用以下命令启动本地摄像头调试模式：

```bash
python app_main.py --local-camera
```

### 指定摄像头索引

如果你的电脑有多个摄像头，可以使用 `--camera-index` 参数指定要使用的摄像头索引：

```bash
python app_main.py --local-camera --camera-index 1
```

默认摄像头索引为 0，通常对应电脑内置摄像头。

## 工作原理

当启用本地摄像头调试模式时：

1. 系统会导入并初始化 `local_camera_debug.py` 模块
2. 创建一个后台线程专门处理本地摄像头视频流
3. 摄像头捕获的帧会被编码为JPEG格式
4. 编码后的视频帧通过 `bridge_io.py` 提供给系统的其他模块
5. 系统其余部分的工作方式与连接眼镜时完全相同

## 调试提示

1. **检查摄像头权限**：确保你的电脑允许Python程序访问摄像头
2. **调整分辨率**：如果需要修改摄像头分辨率，可以直接编辑 `local_camera_debug.py` 文件中的 `CAMERA_WIDTH` 和 `CAMERA_HEIGHT` 变量
3. **帧率控制**：默认限制为15FPS以节省资源，可在 `local_camera_debug.py` 中修改 `FPS_LIMIT` 变量
4. **观察日志**：系统启动时会显示调试模式信息和摄像头状态

## 常见问题

### 1. 摄像头无法打开

- 检查摄像头索引是否正确
- 确保没有其他程序占用摄像头
- 检查系统摄像头权限设置

### 2. 画面质量不佳

- 调整摄像头分辨率参数
- 提高 `local_camera_debug.py` 中的JPEG质量参数
- 确保光线充足

### 3. 系统响应迟缓

- 降低摄像头分辨率
- 减少 `FPS_LIMIT` 值以降低处理频率
- 关闭不必要的后台程序以释放系统资源

## 高级用法

### 独立运行摄像头

你也可以单独运行摄像头模块进行测试：

```bash
python local_camera_debug.py
```

这将启动摄像头并将视频流推送到 `bridge_io`，但不会启动完整的应用程序。

### 自定义摄像头参数

编辑 `local_camera_debug.py` 文件可以调整以下参数：

- `CAMERA_INDEX`：摄像头索引
- `CAMERA_WIDTH` 和 `CAMERA_HEIGHT`：视频分辨率
- `FPS_LIMIT`：帧率限制

## 注意事项

1. 本地摄像头模式主要用于开发和调试，某些依赖眼镜特定传感器（如IMU）的功能可能无法正常工作
2. 确保在正式使用时切换回标准模式（不使用 `--local-camera` 参数）
3. 调试完成后，按Ctrl+C可以安全退出程序，系统会自动释放摄像头资源

---

通过这种方式，你可以在开发和调试阶段快速测试视觉识别和导航功能，而无需依赖实际的ESP32眼镜硬件。